# Paperless

Planned location for the Paperless stack.

Recommended files for this folder:

- `docker-compose.yml`
- `.env.example`
- `.env` on the Docker VM only
- `consume/`, `data/`, `media/`, and `export/` for runtime files ignored by git
