# Plex

Planned location for the Plex stack.

Recommended files for this folder:

- `docker-compose.yml`
- `.env.example`
- `.env` on the Docker VM only
- `config/` and media mount points handled outside git
