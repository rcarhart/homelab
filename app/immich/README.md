# Immich

Planned location for the Immich stack.

Recommended files for this folder:

- `docker-compose.yml`
- `.env.example`
- `.env` on the Docker VM only
- `library/` for photos and videos
- `postgres/` or other runtime data folders ignored by git
